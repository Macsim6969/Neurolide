export interface QuestionsData {
  icon: string
  title: string
  description: string
}

export interface QuestionForm{
  title: string
  name: string
  email: string
  textArea: string
  file: string
  send: string
}